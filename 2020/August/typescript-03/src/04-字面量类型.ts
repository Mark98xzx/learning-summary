function setPosition(ele: Element, direction: 'left' | 'top' | 'right' | 'bottom') {
    //
}

let box0 = document.querySelector('div');
if (box0) {
    setPosition(box0, 'left');
    // setPosition(box0, 'a');
}